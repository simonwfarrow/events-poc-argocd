apiVersion: v1
kind: Service
metadata:
  name: rabbitmq-service
  labels:
    app: rabbitmq-service
    tier: web
spec:
  ports:
  - port: 15672
    targetPort: 15672
  selector:
    app: rabbitmq
    tier: web 